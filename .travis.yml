language: node_js

node_js:
  - '12'

script:
  - npm test
  - npm run build

before_deploy:
  - npm install netlify-cli -g
  - yarn build

deploy:
  provider: script
  edge: true
  script: netlify deploy --prod --dir ./build
  on:
    branch: main
