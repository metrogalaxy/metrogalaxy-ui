import { ToastContainer } from 'react-toastify';
import styled from 'styled-components/macro';

export const StyledToastedContainer = styled(ToastContainer)`
  .Toastify__toast-body {
    font-family: 'Acrom';
    font-size: 1.2rem;
  }
`;
